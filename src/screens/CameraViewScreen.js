import React from 'react';
import {Text} from 'react-native';
import Screen from './Screen';

function CameraViewScreen({}) {
  return (
    <Screen>
      <Text style={{color: 'white'}}>CameraViewScreen</Text>
    </Screen>
  );
}

export default CameraViewScreen;
