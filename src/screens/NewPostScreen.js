import React from 'react';
import {Text} from 'react-native';
import Screen from './Screen';

function NewPostScreen({}) {
  return (
    <Screen>
      <Text style={{color: 'white'}}>NewPostScreen</Text>
    </Screen>
  );
}

export default NewPostScreen;
